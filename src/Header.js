import React, { useState } from "react";


const Header = () => {
    return (
        <div className="header">
            Search by address, GPS coordinates, hotspot name, or click on the map to see the minimum witness distance of a given location.
        </div>
    );
};

export default Header;